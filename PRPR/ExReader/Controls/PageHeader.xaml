<UserControl
    x:Class="PRPR.ExReader.Controls.PageHeader"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    VerticalAlignment="Stretch"
    VerticalContentAlignment="Stretch"
    HorizontalAlignment="Stretch"
    Background="{ThemeResource SystemControlBackgroundChromeMediumBrush}"
    HorizontalContentAlignment="Stretch"
    mc:Ignorable="d"
    Height="48"
    d:DesignHeight="300"
    d:DesignWidth="400"
    >

    <Grid x:Name="grid" Background="{x:Bind Background}" Margin="0,-40,0,0" Padding="0,40,0,0">
        <Grid x:Name="titleBar">
            <ContentPresenter x:Name="content"
                              VerticalAlignment="{x:Bind VerticalContentAlignment}"
                              HorizontalAlignment="{x:Bind HorizontalContentAlignment}"
                              Margin="{x:Bind Padding}"
                              Content="{x:Bind HeaderContent}"/>
        </Grid>
    </Grid>
</UserControl>
